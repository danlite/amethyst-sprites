<%= render :partial => 'pokemon/pokemon_title', :locals => {:title_pokemon => @pokemon, :current_series => true, :series => @pokemon.current_series, :all_series => false } %>

<% if !@pokemon.current_series %>
  <%= link_to "Reserve this sprite", claim_pokemon_path(@pokemon), :class => 'btn btn-success' unless (current_artist.nil? or current_artist.has_maximum_wip) %>
<% end %>

<%= render :partial => 'comments/setup' %>